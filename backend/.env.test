# Test Environment Configuration
NODE_ENV=test

# Database Configuration (localhost pour tests locaux)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blablabook_test
DB_USER=bobby
DB_PASSWORD=othello

# Redis Configuration (localhost pour tests locaux)
REDIS_URL=redis://localhost:6379

# Sessions
SESSION_SECRET=test-session-secret-for-testing

# PostgreSQL Environment
POSTGRES_DB=blablabook_test
POSTGRES_USER=bobby
POSTGRES_PASSWORD=othello